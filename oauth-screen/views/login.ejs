<!DOCTYPE html>
<html>
<head>
  <title>Sign in - Google Accounts</title>
  <style>
    :root { --blue: #1a73e8; --gray: #5f6368; }
    body { font-family: 'Google Sans', Arial, sans-serif; max-width: 450px; margin: 2rem auto; padding: 0 20px; }
    .header { text-align: center; margin-bottom: 2rem; }
    .logo { height: 24px; margin-bottom: 1rem; }
    .card { border: 1px solid #dadce0; border-radius: 8px; padding: 40px 40px 36px; }
    h1 { font-size: 24px; font-weight: 400; margin-bottom: 2rem; }
    .form-group { margin-bottom: 1.5rem; }
    input { width: 100%; padding: 13px 15px; border: 1px solid #dadce0; border-radius: 4px; font-size: 16px; }
    button { background: var(--blue); color: white; border: none; padding: 12px 24px; border-radius: 4px; font-weight: 500; cursor: pointer; }
    .error { color: #d93025; margin-bottom: 1rem; }
    .google-btn { background: white; border: 1px solid #dadce0; color: var(--gray); padding: 12px 24px; border-radius: 4px; cursor: pointer; display: flex; align-items: center; justify-content: center; width: 100%; }
    .google-btn img { height: 18px; margin-right: 10px; }
  </style>
</head>
<body>
  <div class="header">
    <img src="https://www.ping-browser.com/_next/image?url=%2Fping-logo.png&w=48&q=75" alt="Ping" class="logo">
  </div>
  
  <div class="card">
    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <h1>Sign in</h1>

    <!-- Manual Login Form -->
    <form method="POST" action="/login">
      <input type="hidden" name="client_id" value="<%= client_id %>">
      <input type="hidden" name="redirect_uri" value="<%= redirect_uri %>">
      <input type="hidden" name="state" value="<%= state %>">
      
      <div class="form-group">
        <input type="email" name="email" placeholder="Email or phone" required>
      </div>
      
      <div class="form-group">
        <input type="password" name="password" placeholder="Password" required>
      </div>
      
      <div style="display: flex; justify-content: space-between; align-items: center">
        <a href="#" style="color: var(--blue); text-decoration: none">Forgot password?</a>
        <button type="submit">Next</button>
      </div>
    </form>

    <hr style="margin: 1.5rem 0; border: none; border-top: 1px solid #dadce0;">

    <form method="GET" action="/auth/google">
      <button type="submit" class="google-btn">
        <img src="https://banner2.cleanpng.com/20180413/rfe/avfci721i.webp" alt="Google Logo">
        Sign in with Google
      </button>
    </form>

  </div>
</body>
</html>
